# 🗺️ Product Roadmap

## 📝 计划中 (Planned)

### 三、进阶功能

- [ ] **后端：AI 审核意见**（LLM 二次审查译文质量与风格）
  - [ ] 对已翻译分段发起第二轮 LLM 审核请求，产出结构化审核意见（问题描述/修改建议）
  - [ ] 审核上下文注入 TM 匹配结果与相关术语（TM 未就绪时上下文可为空，接口预留）
  - [ ] 更新 `docs/back` 审核接口约定
  - 验收：审核完成后按分段查询到结构化意见；无 TM 数据时审核仍可用

- [ ] **前端：AI 审核意见展示**（按分段展示审核结果）
  - [ ] 分段级审核意见展示、问题标记、建议查看
  - 验收：执行审核后界面按分段展示意见与建议

- [ ] **后端：翻译记忆库(TM)与历史对比、差异告警**（TM 存储匹配 + 对比参考）
  - [ ] TM 存储：历史译文入库与相似度模糊匹配接口
  - [ ] 历史翻译对比接口：返回相似历史译文供参考
  - [ ] TM 差异告警：译文与相似历史译文差异过大时标记告警
  - [ ] 更新 `docs/back` TM 接口约定
  - 验收：录入历史译文后可被匹配到（含相似度）；差异过大的译文被标记告警；按约定文档调用对比接口返回历史译文

- [ ] **前端：历史翻译对比与差异告警界面**（相似译文参考 + 对比视图）
  - [ ] 相似历史译文展示、差异对比视图、告警提示
  - 验收：匹配到历史译文时展示对比内容，差异过大可见告警标记

- [ ] **后端：硬性规则检查引擎**（可扩展确定性规则 + 内置占位符/漏翻检查）
  - [ ] 规则引擎框架：规则独立于核心流程，可配置、可新增而无需改动核心代码
  - [ ] 内置占位符保留检查：校验特殊标记/代码符号未被破坏，含成对性校验（`<xN>`/`</xN>` 配对、顺序、自闭合）
  - [ ] 内置漏翻检测：发现未翻译/缺失的分段
  - [ ] 检查结果接口：违规类型、定位分段/位置
  - [ ] 更新 `docs/back` 规则检查接口约定
  - 验收：破坏占位符的译文被检出、空译文分段被检出；新增一条规则不改动核心流程即可生效

- [ ] **后端：任务管理与开放 REST 接口**（任务生命周期 + 对外 API）
  - [ ] 任务生命周期管理：创建/处理中/完成/失败，进度统计（分段完成数/总数；受保护块不计入分段）
  - [ ] 分段数据模型为公共契约：分段文本（含 `<xN>` 引用）+ 标记表 + 受保护块 + 章归属（epub）；译文保存只存可译文本，标记表不可变
  - [ ] 译文人工编辑保存 API（供编辑器调用，保存分段译文与确认状态）
  - [ ] 对外开放：第三方系统通过同一套 API 创建任务、查询状态、获取译文结果，无内外部接口区分
  - [ ] 更新 `docs/back` 任务接口约定
  - 验收：任务状态按流程流转且进度正确；第三方按文档独立完成「创建任务→翻译→编辑→获取结果」全链路；`docs/back` 文档可直接指导外部对接

- [ ] **前端：翻译编辑器基础**（分段编辑、保存、确认状态）
  - [ ] 分段列表、译文编辑与保存（按 `docs/back` 译文保存约定）、翻译确认状态管理；占位符 `<xN>` 以可读样式渲染、hover 查看标记含义，译文编辑不可删除/改写占位符（违规由规则引擎标记）
  - [ ] 批量操作与基础快捷键
  - 验收：编辑保存后刷新数据不丢失；确认状态可追踪并反映到任务进度

- [ ] **前端：翻译编辑器增强**（集成规则检查、TM 参考、术语提示）
  - [ ] 编辑器内展示规则违规标记（占位符破坏/漏翻）、TM 相似历史译文参考、术语对齐提示
  - 验收：编辑器内违规分段可见标记，有参考译文与术语提示的分段可见对应信息

- [ ] **前端：任务管理与进度看板、Token 管理页**（任务列表 + 看板 + 后台管理）
  - [ ] 任务列表/创建入口、状态筛选、进度看板（完成度百分比展示）
  - [ ] API Token 管理页：生成、撤销、列表展示（配合后端 Token 管理 API）
  - 验收：创建任务后看板进度随翻译/编辑推进更新；Token 管理页可生成新 Token 并在撤销后立即失效

### 四、Docker 容器化

- [ ] **后端：Docker 容器化**（后端镜像 + compose 编排）
  - [ ] 后端多阶段构建 Dockerfile；docker-compose 编排（后端 + 前端镜像服务 + SQLite 数据卷）
  - [ ] `INITIAL_TOKEN` 通过环境变量注入容器
  - [ ] AI 提供商/模型配置经配置管理 API 写入（ADR-0003：密钥存 SQLite，不再经环境变量注入；容器部署后经 API 配置，或提供可选首启种子机制）
  - 任务描述：本机无 Docker 环境，不做本地测试；镜像正确性由后续 GitHub Actions 云端构建验证
  - 验收：Dockerfile/compose 文件齐全且可被 GHA 云端成功构建后端镜像；compose 含 SQLite 持久化卷与初始 Token 环境变量；容器内可经配置管理 API 配置 AI 提供商/模型并触发翻译

- [ ] **前端：前端容器镜像**（nginx 托管 WASM）
  - [ ] 前端 nginx Dockerfile：构建 WASM 产物并托管静态资源，SPA 路由配置
  - 验收：Dockerfile 齐全且可被 GHA 云端成功构建前端镜像

### 五、GitHub Actions 自动发布

- [ ] **后端：GHA CI 与后端镜像发布**（自动构建测试 + 推 GHCR）
  - [ ] push/PR 自动触发构建与测试（CI）
  - [ ] 主分支/tag 推送后构建 Docker 镜像并推送至 GHCR（CD）
  - 验收：推送代码后 Actions 运行通过，GHCR 出现后端镜像

- [ ] **前端：GHA 前端镜像发布**（构建 WASM + 推 GHCR）
  - [ ] 推送后自动构建 WASM 与 nginx 镜像并推送至 GHCR
  - 验收：推送代码后 Actions 运行通过，GHCR 出现前端镜像

### 六、客户端开发（桌面端，纯前端任务）

- [ ] **前端：MAUI 客户端框架搭建**（Blazor Hybrid 骨架 + 复用共享层）
  - [ ] .NET MAUI Blazor Hybrid 项目骨架，接入共享 RCL 组件
  - [ ] 后端地址与 Token 配置、登录流程（复用同一套 REST API）
  - 验收：桌面端可启动、登录后端并进入主界面

- [ ] **前端：客户端核心功能移植**（编辑器、任务管理、导入导出）
  - [ ] 移植翻译编辑器、任务管理、文件导入导出（复用浏览器端已实现功能与共享组件）
  - 验收：桌面端完成「导入→翻译→编辑→导出」全流程

- [ ] **前端：客户端收尾与打包**（看板、设置、发布产物）
  - [ ] 进度看板、设置页移植
  - [ ] 发布打包：产出本机可安装/运行的发布产物
  - 验收：看板可用；打包命令可产出发布产物

### 七、其他（兜底）

## 🚧 开发中 (In Progress)

- [ ] **后端：术语表管理 API 与术语对齐检查**（术语 CRUD + 对齐校验接口）
  - [ ] 术语条目增删改查与模糊匹配查询接口（供翻译/规则引擎复用）
  - [ ] 术语对齐检查接口：校验译文是否使用规定术语，输出未对齐分段
  - [ ] 更新 `docs/back` 术语接口约定
  - 验收：术语 CRUD 生效；包含规定术语的译文通过检查，未使用术语的译文被检出并指出期望术语

- [ ] **前端：术语表管理界面**（术语 CRUD + 搜索）
  - [ ] 术语列表、搜索、新增/编辑/删除操作界面
  - 验收：界面完成术语增删改查，刷新后数据一致

## ✅ 已完成 (Completed)

- [x] **后端：AI 连接器迁移 OllamaSharp**（替换弃用的 `Microsoft.Extensions.AI.Ollama`；二审遗留项，迁移已获批准）
  - [x] 移除 NuGet `Microsoft.Extensions.AI.Ollama`（9.7.0-preview.1.25356.2，官方已标记弃用并推荐 OllamaSharp；无 stable 版）
  - [x] `AiChatClientFactory` 的 Ollama 分支改由 OllamaSharp（`OllamaApiClient`）构造客户端；OllamaSharp 直接实现 `IChatClient`，最小子类仅将其 HTTP 400 `OllamaException` 归一为带状态码的 `HttpRequestException`，不改变翻译处理器
  - [x] 非 400 HTTP 错误改由翻译处理器按真实状态映射，修正旧连接器将全部状态降级为可重试 `llm_response_invalid` 的行为，使错误码与重试语义符合 `docs/back/AI 翻译接口约定.md` §10.2
  - [x] 同步 NuGet 约束：Ollama 连接器依赖 `OllamaSharp`；`docs/back` 配置契约不改（`kind` 仍为 `ollama`）
  - 验收：构建零警告；Api.Tests 全绿（含多 Ollama 提供商端点隔离、连接器与 HTTP 状态/错误码/重试矩阵用例）；`Microsoft.Extensions.AI.Ollama` 不再出现在任何项目依赖中；配置 `kind=ollama` 提供商可实际翻译

### 一、前后端基本框架

- [x] **前端：搭建 Blazor WASM 框架、共享组件库与 Token 登录**（网页端骨架 + 登录闭环）
  - [x] Blazor WebAssembly 项目骨架：路由、布局、导航菜单
  - [x] Razor Class Library 共享组件库：基础组件（按钮/表格/弹窗等）与 `docs/front` 界面规范文档
  - [x] Token 登录界面：输入并保存 Token（本地持久化），全局 HTTP 客户端自动附加鉴权头，401 自动跳转登录
  - [x] 调用后端健康检查接口验证前后端连通
  - 验收：浏览器打开页面→输入 dev Token→进入主界面；清空 Token 后访问自动跳登录；共享组件渲染正常；`docs/front` 存在界面规范文档

- [x] **后端：搭建 Web API 框架与 Token 鉴权**（API 骨架 + SQLite + 多 Token 鉴权体系）
  - [x] 创建 ASP.NET Core Web API 项目骨架：统一 `/api` 路由前缀、统一错误响应格式、Swagger 文档
  - [x] 接入 SQLite 数据库与迁移机制，建立 Token 存储表
  - [x] 全部 `/api` 接口默认要求 `Authorization: Bearer sk-{32位guid}`（公开白名单接口除外），Token 存库校验
  - [x] Token 管理 API：生成、撤销、列表（供外部系统申请、可单独吊销）
  - [x] 初始 Token 由部署环境变量 `INITIAL_TOKEN` 注入，作为首条 Token 记录；dev 环境未配置时自动启用固定开发 Token（`sk-dev-` 前缀），日志提示，鉴权行为与生产一致
  - [x] 编写 `docs/back` 接口约定文档：Token 获取方式、请求头格式、错误码约定
  - 验收：curl 以有效 Token 访问受保护接口返回 200，无 Token / 伪造 Token 返回 401；生成→使用→撤销后立即失效；未配置 `INITIAL_TOKEN` 时 dev 固定 Token 可直接使用；`docs/back` 存在鉴权约定文档

### 二、基础功能

- [x] **后端：文件导入拆解与导出**（txt/markdown/html 解析 + 标记提取回填 + 重提取调整）
  - [x] 支持格式：txt、markdown、html（html 文件 / epub / URL）；接口显式 `fileType` 参数，缺省按扩展名/Content-Type 推断，推断失败返回 400
  - [x] txt 分段：段落式（空行分隔，软换行保留）或换行式（按物理行）；导入可选，未指定时智能推荐（含空行块 → 段落式，几乎无空行 → 换行式）；超长段落不切分
  - [x] markdown：空行分段基础上，行首块级标记（标题/列表项/引用/表格分隔符）逐行提取为占位符；代码块、分隔线、内嵌 HTML 块为受保护块（不译）
  - [x] html 提取：默认提取 body 可译内容，确定性剔除 script/style/noscript/template/iframe/svg；块级标签定分段边界；全部标签与实体按 ADR-0002 占位符化（成对 `<xN>`/`</xN>`/`<xN/>`，标记表存原文与含义提示）；属性不译；pre/code 不译
  - [x] epub：章 = spine 中的 XHTML 项，逐章走 html 管道，分段带章归属；目录（nav.xhtml）参与翻译；OPF 元数据/CSS/图片/字体不译、原样字节拷贝
  - [x] URL 导入：后端静态 GET（浏览器 UA、跟随重定向、超时、大小上限、编码检测）；JS 渲染页明确报错不支持；导出 = 下载还原后的 html，不回写网页
  - [x] 重提取（html 类任务级操作）：CSS selector 指定可译范围 + 分段预览 + 确认后替换分段；存在已翻译/已确认分段时警告「将丢失译文」并需显式确认；selector 无匹配报错；范围外内容保留为受保护块
  - [x] 导出回填：txt/markdown/html 逐字节还原（含重提取后，范围外内容原样保留）；epub 重建 zip（mimetype 首位无压缩、未改动资源字节拷贝、已译章回填替换）
  - [x] 更新 `docs/back` 文件导入导出接口约定（分段契约：文本 + 标记表 + 受保护块 + 章归属）
  - 验收：txt/md/html 样例分段正确且占位符成对完整；txt/md/html 导出与原文件字节一致，epub 可被阅读器打开且未译资源字节一致；URL 任务可创建并导出；重提取可调整分段范围且导出仍为完整文件；按约定文档调用 API 可完成 导入→重提取→导出

- [x] **前端：文件导入导出界面**（上传/URL 创建任务 + 分段预览 + 下载导出）
  - [x] 文件上传（txt/markdown/html/epub）与 URL 输入创建翻译任务，展示分段预览列表
  - [x] txt 分段模式选择（段落式/换行式，展示智能推荐默认值）
  - [x] 分段预览展示成对占位符 `<xN>`，可查看标记含义（如 `&nbsp;` = 不间断空格）
  - [x] html 类任务重提取界面：selector 输入 + 提取预览 + 覆盖确认（存在已翻译/已确认分段时警告「将丢失译文」）
  - [x] 导出入口：任务完成后下载回填还原的文件
  - 验收：浏览器上传文件/输入 URL → 看到分段列表（txt 按所选模式）→ 翻译后下载的文件与上传文件格式一致；重提取可调整分段范围且预览与实际结果一致

- [x] **后端：AI 全自动翻译**（可插拔 LLM 调用 + 分段批量翻译）
  - [x] LLM API 可插拔配置（BaseUrl/Key/模型），不绑定具体厂商
  - [x] 对分段文本批量翻译，占位符保护（翻译过程中占位符不进入/不丢失；占位符引用按 ADR-0002 格式 `<xN>`/`</xN>`/`<xN/>`，AI 不得改写/删除/新增）
  - [x] 失败重试与部分失败处理，任务状态正确反映翻译进度
  - [x] 更新 `docs/back` AI 翻译接口约定
  - 验收：配置 LLM 后触发翻译，全部分段获得译文且占位符完整；LLM 不可用时返回明确错误且任务标记失败；按约定文档调用 API 可触发并查询翻译

- [x] **前端：AI 翻译操作界面**（触发翻译 + 状态与结果展示）
  - [x] 触发翻译按钮、翻译进度/状态展示、失败错误提示
  - 验收：点击翻译后分段逐条获得译文，翻译中可见进度，失败可见明确提示

- [x] **后端：任务列表接口**（任务列表 + 进度统计）
  - [x] `GET /api/tasks` 分页列表：按创建时间倒序，含状态、分段进度（完成数/总数）、最新翻译运行摘要
  - [x] 状态筛选，复用既有游标分页与错误码约定
  - [x] 更新 `docs/back` 文件导入导出接口约定（补充任务列表接口）
  - 验收：调用列表接口返回全部任务与进度；前端任务页可加载列表并进入详情

- [x] **前端：任务列表界面**（首页任务列表 + 创建入口）
  - [x] 首页展示任务列表：状态徽标（三通道）、进度（完成分段数/总数）、创建时间；点击行进入任务详情
  - [x] 任务创建入口（上传/URL）与空态、错误态；状态筛选（创建/处理中/完成/失败）
  - 验收：进入首页看到任务列表与进度，可创建任务并进入详情；刷新后数据一致

- [x] **后端：AI 提供商与模型配置管理 API**（二级配置 + Microsoft.Extensions.AI 执行层迁移，见 ADR-0003）
  - [x] 二级配置模型：Provider（kind/baseUrl/apiKey/enabled，v1 支持 `openai` 与 `ollama` 连接器）→ Model（厂商模型 ID/展示名/能力元数据 supportsThinking/supportsToolUse/supportsStreaming）；Provider.isDefault 全局唯一、Model.isDefault 提供商内唯一合成默认对
  - [x] 配置 CRUD API：提供商与模型增删改查、默认对设置；ApiKey 明文存 SQLite、响应一律掩码（`sk-***abcd`）且不写日志；无默认对时创建翻译运行返回 503 `llm_not_configured`
  - [x] 执行层迁移 Microsoft.Extensions.AI：移除 `ITranslationProvider`，翻译处理器直接消费 `IChatClient`（按 kind 工厂构造；批量提示词、JSON 解析、超时/鉴权错误码映射迁入处理器）；新增 NuGet：`Microsoft.Extensions.AI`、`Microsoft.Extensions.AI.OpenAI`、`Microsoft.Extensions.AI.Ollama`
  - [x] 翻译运行请求新增可选 `providerId`/`modelId`（三档解析：都缺省→默认对；只传 providerId→该提供商默认模型；都传→精确指定）；运行资源回显所选提供商/模型
  - [x] 新增错误码：404 `provider_not_found`、422 `model_not_found`；503 语义收窄为「选中的提供商/模型未配置完整」
  - [x] 更新 `docs/back` AI 翻译接口约定 §3（配置模型与配置管理接口）、§4（请求/响应字段）、§10.1（错误码）
  - 验收：通过 API 配置多个提供商与模型并设默认对；创建翻译运行可显式选择提供商/模型或走默认对；不同提供商/模型可实际翻译；密钥不泄露；无效选择返回对应错误码

- [x] **前端：AI 模型配置界面**（提供商/模型两级管理 + 翻译面板选择器）
  - [x] 提供商/模型两级管理界面：提供商 CRUD（kind/BaseUrl/Key，密钥不回显明文、enabled/默认对设置）+ 模型 CRUD（厂商模型 ID/展示名/能力标注）
  - [x] 翻译操作面板新增提供商/模型选择器（缺省显示默认对），提交翻译时随请求携带（ADR-0003 三档解析）
  - [x] 保存生效提示与配置状态展示（未配置默认对时提示将影响翻译功能）
  - [x] 验收：界面可配置多个提供商与模型、设默认对并保存；翻译面板可选择提供商/模型发起翻译；刷新后配置与选择一致
