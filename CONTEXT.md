# MyTranslator 领域术语表

## 翻译域

- **分段（Segment）**：源文件中可独立翻译的最小文本单元（按段落或按换行拆解，取决于分段模式）。翻译、编辑、检查、状态追踪的基本单位。超长段落不二次切分，原样作为一段。
- **段落（Paragraph）**：由空行（或仅含空白字符的行）分隔的文本块，块内软换行保留；纯空白块不产生分段。
- **分段模式（Segmentation Mode）**：导入时的分段规则——段落式（空行分隔）或换行式（每物理行为一段）。txt 可手动指定；未指定时按文本特征自动推荐（含空行块 → 段落式；几乎无空行 → 换行式）。
- **任务（Task）**：一次"导入文件 → 翻译 → 编辑 → 导出"的完整工作单元。生命周期：创建 → 处理中 → 完成 / 失败。
- **确认状态**：分段译文的人工验收状态：待翻译 → 已翻译（AI 翻译后自动）→ 已确认（人工）。已确认可撤销。
- **占位符（Placeholder）/ 标记（Markup）**：源文本中不可翻译的特殊标记（代码符号、markdown 标记、html 标签、html 实体）。导入时按提取模型（ADR-0002）剥离为段内**成对引用** `<xN>`/`</xN>`/`<xN/>`（OmegaT 风格），标记表记录原文与含义提示；翻译期间原样保留，导出时回填还原。
- **受保护块（Protected Block）**：无可译文本、必须原样保留的内容块（代码块、分隔线、markdown 内嵌 HTML 块等）。不产生分段，导出原位还原。
- **重新提取（Re-extraction）**：html 类来源（html 文件/URL/epub 章）的任务级调整操作——用户指定 CSS 选择器作为提取范围，预览提取结果后确认，以新分段替换旧分段。存在已翻译/已确认分段时须显式确认（将丢失译文）。txt/markdown 无此概念。
- **章（Chapter）**：epub 中 spine 阅读顺序里的一个 XHTML 内容项，是 html 管道的最小处理单元。分段记录所属章，导出按 spine 顺序重组。
- **规则违规（Rule Violation）**：硬性规则检查引擎检出的确定性问题（占位符破坏、漏翻等），不依赖 AI。
- **AI 审核意见（Review Comment）**：第二轮 LLM 对译文质量的审查结果，含问题描述与修改建议。

## 记忆与术语

- **术语 / 术语表（Term / Glossary）**：规定译法的词条集合，翻译时须对齐检查。
- **翻译记忆库（TM）**：历史译文库，提供相似度模糊匹配与差异对比参考。

## 前端域

- **通用组件**：通用能力组件（按钮/表格/弹窗/表单/进度），来自 MudBlazor（ADR-0001）。
- **领域组件**：RCL 自研的 CAT 特有组件（分段列表、详情面板、差异视图、进度看板、违规徽标等）。
- **脏标记（Dirty Mark）**：分段存在未保存改动的可视标记（`●`）。
- **语义色**：与翻译域状态一一映射的主题色 token：success=已确认、warning=TM 告警、error=规则违规、info=AI 审核意见、neutral=未翻译/待确认。状态呈现必须颜色 + 图标 + 文字三通道。

## 用语红线

- 说"确认"指分段的确认状态；任务层面说"完成/失败"。
- 说"违规"指规则引擎检出（占位符破坏、漏翻）；"错误"仅指系统/接口故障。
- 说"审核意见"指 AI 二轮审查产出，不说"评分/报告"。
